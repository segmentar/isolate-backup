using Core;

namespace Core
{
    using System;

    public partial struct MaterialreflectSecondarymodule
    {
        public partial class Tertiaryfunction
        {
            public static void TertiaryfunctionVoidII()
            {
                TertiaryForeachIII();

                FunctionTwentyArrayList.Remove(Left);

                FunctionTwentyArrayList.Remove(Right);

                var start = (Left.Position + 1);

                var end = (Right.Position - 1);

                var difference = (end - start);

                var count = (difference + 1);

                var sub = InputString.Substring(start, count);

                var ReflectStringSafe___VALUE = MaterialStringSafe.Simple(sub);

                var item_FUNCTION = new FunctionThirty(Left, Right, Rank, ReflectStringSafe___VALUE);

                FunctionThirtyArrayList.Add(item_FUNCTION);

                return;
            }
        }
    }
}
