using Core;

namespace Core
{
    using System;

    using System.Collections;

    public partial struct MaterialreflectSecondarymodule
    {
        public partial class Tertiaryfunction
        {
            public static void TertiaryfunctionFunctionII()
            {
                var aoth = InputString.Length;

                foreach (FunctionTwenty value_FUNCTION in new ArrayList(FunctionTwentyArrayList))
                {
                    var boolean_safe_SMALLER_is = true;

                    boolean_safe_SMALLER_is = boolean_safe_SMALLER_is && value_FUNCTION.Character.Equals(Left.SymmetryCharacter) is true;

                    boolean_safe_SMALLER_is = boolean_safe_SMALLER_is && value_FUNCTION.HasSymmetry.Equals(true) is true;

                    boolean_safe_SMALLER_is = boolean_safe_SMALLER_is && (value_FUNCTION.Position > Position) is true;

                    boolean_safe_SMALLER_is = boolean_safe_SMALLER_is && (value_FUNCTION.Position < aoth) is true;

                    Boolean isSmallerCheck, shouldContinueCheck;

                    isSmallerCheck = boolean_safe_SMALLER_is is true;

                    shouldContinueCheck = isSmallerCheck is false;

                    if (shouldContinueCheck is true)
                    {
                        continue;
                    }
                    else
                        "false".ToString();

                    Right = value_FUNCTION;

                    aoth = value_FUNCTION.Position;

                    continue;
                }

                return;
            }
        }
    }
}
